<Window x:Class="SkoprRecord.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SkoprRecord.App.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="Skopr Kaydet" Height="380" Width="480"
        Icon="Assets/app_icon.png"
        WindowStyle="None" ResizeMode="CanMinimize" AllowsTransparency="True" Background="Transparent" WindowStartupLocation="CenterScreen">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" ResizeBorderThickness="0" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Window-specific resources (if needed in future) -->
    </Window.Resources>

    <Border Background="{StaticResource BgDarkBrush}" CornerRadius="16" BorderBrush="#333" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0">
                <TextBlock Text="SKOPR KAYDET" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="20,0,0,0"
                           Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold" FontSize="12"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <!-- Settings Button -->
                    <Button Content="⚙" Width="40" Height="40"
                            Background="Transparent" Foreground="{StaticResource TextPrimaryBrush}" BorderThickness="0"
                            Click="SettingsButton_Click" WindowChrome.IsHitTestVisibleInChrome="True"
                            ToolTip="Ayarlar">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#444"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Minimize Button -->
                    <Button Content="─" Width="40" Height="40"
                            Background="Transparent" Foreground="{StaticResource TextPrimaryBrush}" BorderThickness="0"
                            Click="MinimizeButton_Click" WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#444"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Close Button -->
                    <Button Content="✕" Width="40" Height="40"
                            Background="Transparent" Foreground="{StaticResource TextPrimaryBrush}" BorderThickness="0"
                            Click="CloseButton_Click" WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#CC4444"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Main Content -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,-10,0,0">

                <!-- Status Indicator -->
                <Border Background="{StaticResource BgLightBrush}" CornerRadius="20" Padding="15,5" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="10" Height="10" Fill="{Binding StatusColorBrush}" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <!-- Time Display -->
                <TextBlock Text="{Binding ElapsedTime}" FontSize="42" Foreground="{StaticResource TextPrimaryBrush}"
                           FontFamily="Consolas, Monospace" HorizontalAlignment="Center" Margin="0,0,0,25"
                           Opacity="0.9">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                    </TextBlock.Effect>
                </TextBlock>

                <!-- Monitor & Mode Selection -->
                <Border Background="{StaticResource BgLightBrush}" CornerRadius="10" Padding="15,10" HorizontalAlignment="Center" Margin="0,0,0,25" Width="380">
                    <StackPanel>
                        <!-- Monitor Selection -->
                        <Grid Margin="0,0,0,10" Visibility="{Binding MonitorSelectionVisibility}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="🖥️ Monitör:" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,10,0"/>

                            <!-- ✅ Sadece bu ComboBox koyu -->
                            <ComboBox Grid.Column="1"
                                      ItemsSource="{Binding AvailableMonitors}"
                                      SelectedItem="{Binding SelectedMonitor}"
                                      IsEnabled="{Binding CanSelectMonitor}"
                                      Height="30"
                                      Style="{StaticResource DarkComboBoxStyle}"
                                      ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DeviceName}" />
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Audio Settings -->
                <Border Background="{StaticResource BgLightBrush}" CornerRadius="10" Padding="15,10" HorizontalAlignment="Center" Margin="0,0,0,25">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding CaptureSystemAudio}" IsEnabled="{Binding CanChangeSettings}"
                                  Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,20,0">
                            <TextBlock Text="🔊 Sistem Sesi" Foreground="{StaticResource TextPrimaryBrush}" FontSize="12"/>
                        </CheckBox>
                        <CheckBox IsChecked="{Binding CaptureMicrophone}" IsEnabled="{Binding CanChangeSettings}"
                                  Foreground="{StaticResource TextPrimaryBrush}">
                            <TextBlock Text="🎤 Mikrofon" Foreground="{StaticResource TextPrimaryBrush}" FontSize="12"/>
                        </CheckBox>
                    </StackPanel>
                </Border>

                <!-- Controls -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Command="{Binding StartScreenRecordingCommand}" Visibility="{Binding StartButtonsVisibility}" Content="EKRAN KAYDI" Style="{StaticResource PrimaryBtn}" Margin="0,0,10,0" Width="130" Height="45"/>
                    <Button Command="{Binding StartAudioRecordingCommand}" Visibility="{Binding StartButtonsVisibility}" Content="SES KAYDI" Style="{StaticResource PrimaryBtn}" Margin="0,0,10,0" Width="130" Height="45" Background="#2196F3"/>
                    <Button Command="{Binding StopRecordingCommand}" 
                            Content="{Binding StopButtonText}" 
                            Background="{Binding StopButtonBackground}"
                            Style="{StaticResource ModernBtn}" 
                            Width="120" Height="45"/>
                </StackPanel>

                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#FF6B6B" TextWrapping="Wrap" Margin="0,15,0,0" HorizontalAlignment="Center" FontSize="12"/>

            </StackPanel>
        </Grid>
    </Border>
</Window>
